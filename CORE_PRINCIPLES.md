# AI_Trader コア原則

## 1. 設計原則

### SSOT（Single Source of Truth）
- 全設定値は `config/settings.yaml` に一元管理
- ハードコーディング禁止：マジックナンバー、接続文字列、パラメータは全て設定ファイルまたは環境変数から取得
- 定数は `config/constants.py` で定義

### モジュール化
- 各モジュールは単一責務を持つ
- モジュール間の依存は明示的インターフェースを通じて行う
- 循環依存の禁止

### ドキュメンテーション
- 全関数・クラスにdocstringを記述（日本語）
- 型ヒント（Type Hints）を必須とする
- 複雑なロジックにはインラインコメントを付与

## 2. データ整合性原則

### 未来情報参照の絶対禁止（データリーク防止）
- テクニカル指標計算時に、未来の価格データが混入しないことを保証
- パージング（Purging）とエンバーゴ（Embargo）を全検証で適用
- `look-ahead bias` チェックを自動テストに組み込む

### タイムゾーン統一
- 内部処理は全て **UTC** で統一
- 外部データ取得時に必ず UTC 正規化を実施
- HistData（EST/DST非対応）からの変換ロジックを厳格に実装

### データ使い捨て原則
- テストデータでの結果が悪い場合、ロジック微調整後に同じデータで再テストすることは禁止
- データセットの分割は一度決定したら変更不可

## 3. リスク管理原則

### バルサラ破産確率
- 破産確率が数学的に 0% となるポジションサイズのみ許可
- 毎トレード前にリアルタイムで破産確率を再計算

### ATRベース動的リスク
- 損切り・利確距離はATR（Average True Range）に基づく
- 固定pips値でのストップロスは禁止

### モンテカルロ検証
- ライブ稼働前に最低1万回のシミュレーションを実施
- 最大ドローダウンの確率分布を可視化し承認を得ること

## 4. モデル検証原則

### オーバーフィッティング防止
- PF/PFr（最適化PF / ランダムPF）による偶然排除
- 組合せパージング交差検証（CPCV）の必須適用
- パラメータの「なだらかな山の中心」選定

### モデル複雑度の制限
- 説明可能なバイアス-バリアンスのトレードオフ範囲内に留める
- 不必要な層・ニューロンの追加を避ける
- 正則化（Dropout, L2）を必ず適用

## 5. 執行原則

### 約定前提
- バックテスト・実運用ともに「次足始値」での約定を前提
- スプレッドとスリッページを厳格にモデル化

### スプレッド監視
- エントリー前にリアルタイムスプレッドを確認
- 閾値超過時はエントリー見送り

## 6. 運用原則

### 24時間稼働
- プロセス死活監視は独立スレッドで実行
- 異常検知時は即座にプッシュ通知 + メール送信
- Windows再起動後の自動復帰を保証

### ログ管理
- 全取引をログファイルに記録
- エラーログは別ファイルで管理
- ログローテーションを実装
